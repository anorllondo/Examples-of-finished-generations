🚀 Примеры генерации (Case Studies)
В данном разделе представлены реальные результаты работы системы Figma-to-React. Все интерфейсы были сгенерированы автоматически с использованием IDE Cursor + Claude 3.5 Sonnet и данных, полученных через @figma-context MCP server.

🥇 Case 1: Complex Landing Page (Digital Marketing)
Статус: Active | Сложность: Высокая | Стек: React 19 + Grid Layout

Финальный результат работы системы: Полноценный корпоративный сайт агентства с сложной сеткой, адаптивностью и автоматической загрузкой ассетов.

💬 Использованный промпт:

Вводные данные:
Макет: [LINK_TO_FIGMA_ZEBRA_AGENCY]
Имя модуля: DigitalMarketing

Задача: Выполни полный цикл замены лендинга.
1. Анализ и Ассеты: Скачай картинки в public/assets/DigitalMarketing/.
2. Архивация: Убери текущий активный сайт в архив.
3. Разработка: Сгенерируй модуль. Обязательно декомпозируй на: Navbar, Hero, Services, WhyChooseUs, LatestWork, Testimonials, Footer.
   - Используй CSS Grid для Hero секции (избегай Layout Shift).
4. Сборка: Подключи модуль в App.tsx.
5. Тесты: Напиши E2E тест digital-marketing.spec.ts.

⚡ Сгенерированные компоненты:

Navbar.tsx (Адаптивное меню)
Hero.tsx (Grid-верстка текста и изображения)
Services.tsx (Карточки с hover-эффектами)
WhyChooseUs.tsx (Список преимуществ)
LatestWork.tsx (Портфолио)
Footer.tsx

🥈 Case 2: Portfolio Landing (Makeup Artist)
Статус: Archived | Сложность: Средняя | Особенность: Работа с графикой

Пример генерации лендинга с большим количеством изображений. Система автоматически использовала скрипт download_figma_images для извлечения фотографий из макета и их оптимизации.

💬 Использованный промпт:

Вводные данные:
Макет: [LINK_TO_FIGMA_MAKEUP]
Имя модуля: MakeupArtist

Задача: Сгенерируй портфолио визажиста.
1. Анализ и Ассеты:
   - Критично: Использовать tool `download_figma_images` для всех фото работ.
   - Сохранять в `public/assets/makeup/`.
2. Архивация: Перемести активный модуль в `src/modules/_archive/`.
3. Разработка:
   - Создай контейнер `HomeLandingMakeUp.tsx`.
   - Декомпозиция: MakeupHero, MakeupServices, PortfolioGrid, Pricing.
   - Стилизация: Используй `bg-clip-text` для золотых градиентов заголовков.
4. Сборка: Обнови App.tsx.
5. Тесты: Сгенерируй `makeup-landing.spec.ts`.

⚡ Сгенерированные компоненты:

HomeLandingMakeUp.tsx (Главный контейнер страницы)
MakeupHero.tsx (Первый экран с фото и градиентным заголовком)
MakeupServices.tsx (Блок услуг с ценами)
PortfolioGrid.tsx (Адаптивная сетка изображений/работ)
Testimonials.tsx (Слайдер отзывов)
Footer.tsx (Подвал с контактами)

🛠 Технические особенности:

Автоматический подбор градиентов для текста (bg-clip-text).
Импорт локальных изображений вместо плейсхолдеров.
Feature-based структура папок.

🥉 Case 3: Isolated Component (Let's Connect)
Статус: Archived | Сложность: Низкая | Особенность: Data-Driven UI

Первый эксперимент по генерации изолированной секции. Демонстрирует способность нейросети выделять повторяющиеся элементы (социальные иконки) в массивы данных.

💬 Использованный промпт:

Вводные данные:
Макет: [LINK_TO_FIGMA_CONNECT]
Имя модуля: ConnectSection

Задача: Реализуй секцию контактов как изолированный компонент.
1. Анализ: Найди повторяющиеся элементы (социальные иконки).
2. Архивация: Очисти App.tsx от предыдущих лендингов.
3. Разработка:
   - Вынеси данные ссылок в типизированный массив `socialLinks` (Data-Driven UI).
   - Используй `.map()` для рендеринга.
   - Не используй хардкод HTML для списка иконок.
4. Сборка: Подключи `<ConnectSection />` в App.tsx.
5. Тесты: Простой тест на видимость заголовка "Let's Connect".

⚡ Сгенерированные компоненты:

ConnectSection.tsx (Единый изолированный компонент)
Примечание: В данном кейсе архитектура намеренно упрощена до одного файла для демонстрации возможности генерации отдельных виджетов, а не целых страниц.
